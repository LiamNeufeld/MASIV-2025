{
  "private": true,
  "scripts": {
    "build": "cd frontend && npx vite build",
    "dev": "cd frontend && npx vite",
    "start": "cd frontend && npx vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.4"
  }
}
